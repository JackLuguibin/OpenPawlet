# Honcho: `honcho start` from repo root (venv activated).
# Ctrl+C stops all processes.
gateway: bash scripts/ensure-nanobot-onboard.sh && nanobot gateway
server: console server
web: console web dev

